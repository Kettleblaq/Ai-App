{
  "name": "ai-app",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js"
  }
}
